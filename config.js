// Configuration file for the game

const APP_NAME = "Escape the Haunted House";

const DEFAULTS = {
    startingLevel: 1,
    startingScore: 0,
    maxLevels: 5,
    maxTime: 30,   // Time per level in seconds
};
